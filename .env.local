# Local development env — not committed (see .gitignore).

# Base URL of the backend REST API (Express + MongoDB, in /backend).
# Must include the /api suffix. Start the backend with `cd backend && npm run dev`.
NEXT_PUBLIC_API_URL=http://localhost:5000/api
